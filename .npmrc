# Better-sqlite3 build configuration for deployment platforms
# This ensures the native module is built correctly

# Use prebuilt binaries when available
build-from-source=false

# If building from source, use these settings
python=python3
